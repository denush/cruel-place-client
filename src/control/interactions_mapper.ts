import type { InteractionsMapper } from "./utils";

export const interactionsMapper: InteractionsMapper = {
  moveUp: { button: "KeyW", category: "move" },
  moveDown: { button: "KeyS", category: "move" },
  moveLeft: { button: "KeyA", category: "move" },
  moveRight: { button: "KeyD", category: "move" },
};
